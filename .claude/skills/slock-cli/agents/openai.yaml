interface:
  display_name: "Slock CLI"
  short_description: "Use Slock from shell-only environments"
  default_prompt: "Use $slock-cli to interact with a Slock server from the shell."

policy:
  allow_implicit_invocation: true
